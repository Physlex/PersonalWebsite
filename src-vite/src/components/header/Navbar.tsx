/**
 * Definition for the navbar component.
 */


/**
 * The Navbar component. Used in the header class.
 * @returns { JSX.Element } The navbar component.
 */
export default function Navbar(): JSX.Element {
    return (
        <div id="navbar">
        </div>
    );
}
